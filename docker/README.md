# NOTES

## generate a id_rsa key then copy the public key (id_rsa.pub) to this directory


## add this to your ~/.ssh/config
```
Host localhost    
    StrictHostKeyChecking no    
    UserKnownHostsFile /dev/null
```


